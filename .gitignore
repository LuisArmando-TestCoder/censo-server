# Env & secrets
.env
.env.local
service-account.json

# Logs
*.log

# OS
.DS_Store
node_modules
# Chrome profile holding the crawler's Google session. Never commit: it is a
# live credential.
.chrome/
